/**
 * Authenticated / app routes (library, auth, settings). Global chrome lives in root `layout.tsx`.
 */
export default function AppShellLayout({ children }: { children: React.ReactNode }) {
  return children;
}
